name: Deploy Speech Service

on:
  push:
    branches: [main]
    paths:
      - 'SpeechService'
      - '.github/workflows/deploy-speech-service.yml'

env:
  IMAGE: ghcr.io/${{ github.repository_owner }}/english-compass-speech-service

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Build with Gradle
        run: |
          cd SpeechService
          ./gradlew clean build -x test

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./SpeechService
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}

      - name: Deploy to EC2 #3 (speech-service)
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_3_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_3_SSH_KEY }}
          script: |
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker pull ${{ env.IMAGE }}:latest
            cd ~/app
            docker-compose up -d --no-deps --force-recreate speech-service
            docker image prune -f
